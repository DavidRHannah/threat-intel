"""
AWS CDK app for launching the platform. 

There is one stack per backend layer.
`app.py` composes the stacks. 
The stacks package holds the stack definitions.
The constructs package holds the reusable pieces shared across them.
"""
