"""
Package for shared code that is used by every layer.

`neo4j_driver.py` supports the graph database connection.
`config.py` supports config detail retrieval.
`natural_keys.py` consists of helper functions for avoiding key duplication.
The `graph` subpackage holds the logic for graph-writes.
"""
