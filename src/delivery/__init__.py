"""
The API that supports the dashboard and other consumers including `dashboard_handler.py`, `search_handler.py`, `ttp_heatmap_handler.py`, and `assets_handler.py`.

All of these are read-only except for `assets_handler.py`, which also handles asset create/update/delete. 
`knobs.py` consists of the settings that may be tuned.
`_response.py` consists of the shared response format.
"""
